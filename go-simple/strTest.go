package main

import (
	"fmt"
)

func main() {
	// s := "len"

	// ss := strings.Split(s, "")

	// for _, splitSS := range ss{
	ex := map[string]string{
		"s": "ss",
		"c": "cc",
	}

	// ex["s"]

	fmt.Println(ex["s"])

	// fmt.Println(len(ss))
}
